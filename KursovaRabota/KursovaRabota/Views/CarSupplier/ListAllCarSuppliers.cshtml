@model ICollection<KursovaRabota.ViewModels.CarSuppliers.CarSupplierDetailViewModel>

@{
    ViewData["Title"] = "Suppliers";
}

<button class="btn btn-primary" onclick="location.href='@Url.Action("EditCarSupplier")'">Create new </button>
<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Supplier</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@Html.ActionLink(@item.SupplierName, "CarSupplierDetails", new { id = item.Id })</td>
                <td>
                    <div class="actions">
                        <button class="btn btn-primary" onclick="location.href='@Url.Action("EditCarSupplier", "CarSupplier", new { id = item.Id })'">Edit</button>
                        <button class="btn btn-primary" onclick="location.href='@Url.Action("DeleteCarSupplier", "CarSupplier", new { id = item.Id })'">Delete</button>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>